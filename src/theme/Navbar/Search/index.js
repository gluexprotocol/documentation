import React from 'react';
import clsx from 'clsx';

import styles from './styles.module.css';

const NavbarSearch = ({ children, className }) => {
  return <>{children}</>;
};

export default NavbarSearch;
